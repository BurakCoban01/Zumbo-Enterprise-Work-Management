export type IntegrationSurface='webhooks'|'development';export interface Webhook{readonly id:string;readonly name:string;readonly targetUrl:string;readonly eventScopes:readonly string[];readonly isActive:boolean;readonly secretFingerprint:string;readonly secretVersion:number;readonly updatedAtUtc:string;readonly version:number}export interface WebhookMetrics{readonly pending:number;readonly processing:number;readonly delivered:number;readonly deadLetter:number}export interface WebhookDelivery{readonly id:string;readonly status:string;readonly eventScope?:string;readonly attempts?:number;readonly createdAtUtc?:string;readonly lastErrorCode?:string|null}export interface SecretReceipt{readonly secret:string;readonly fingerprint:string;readonly version:number}export interface DevelopmentConnection{readonly id:string;readonly name:string;readonly provider:string;readonly baseUrl:string;readonly isConnected:boolean;readonly healthStatus:string;readonly webhookSecretFingerprint:string;readonly webhookSecretVersion:number;readonly version:number}export interface DevelopmentReceipt{readonly connection:DevelopmentConnection;readonly webhookSecret:string}export interface WebhookReceipt{readonly subscription:Webhook;readonly secret?:string}
