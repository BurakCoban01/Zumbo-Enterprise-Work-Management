import{inject,Injectable}from'@angular/core';import{ZumboApiClient}from'@zumbo/modern-shared';import{forkJoin}from'rxjs';import{DevelopmentConnection,DevelopmentReceipt,Webhook,WebhookDelivery,WebhookMetrics,WebhookReceipt}from'./integration.models';@Injectable()export class IntegrationService{private readonly api=inject(ZumboApiClient);load(){return forkJoin({webhooks:this.api.get<readonly Webhook[]>('/api/integrations/webhooks'),metrics:this.api.get<WebhookMetrics>('/api/integrations/webhooks/metrics'),development:this.api.get<readonly DevelopmentConnection[]>('/api/integrations/development')});}deliveries(id:string){return this.api.get<{readonly items:readonly WebhookDelivery[];readonly nextCursor?:string|null}>(`/api/integrations/webhooks/${encodeURIComponent(id)}/deliveries?pageSize=30`);}saveWebhook(draft:{name:string;targetUrl:string;eventScopes:readonly string[]},selected?:Webhook|null){return selected?this.api.put<WebhookReceipt|Webhook>(`/api/integrations/webhooks/${encodeURIComponent(selected.id)}`,{...draft,expectedVersion:selected.version},{ifMatch:selected.version}):this.api.post<WebhookReceipt>('/api/integrations/webhooks',draft);}rotateWebhook(value:Webhook){return this.api.post<{readonly subscription:Webhook;readonly secret:string}>(`/api/integrations/webhooks/${encodeURIComponent(value.id)}/rotate-secret`,{expectedVersion:value.version},{ifMatch:value.version});}setWebhookActive(value:Webhook,active:boolean){return this.api.post<Webhook>(`/api/integrations/webhooks/${encodeURIComponent(value.id)}/${active?'enable':'disable'}`,{expectedVersion:value.version},{ifMatch:value.version});}testWebhook(id:string){return this.api.post<WebhookDelivery>(`/api/integrations/webhooks/${encodeURIComponent(id)}/test-delivery`,{});}replayDelivery(id:string){return this.api.post<WebhookDelivery>(`/api/integrations/webhooks/deliveries/${encodeURIComponent(id)}/replay`,{});}createDevelopment(draft:{name:string;provider:string;baseUrl:string;accessToken:string}){return this.api.post<DevelopmentReceipt>('/api/integrations/development',draft);}health(value:DevelopmentConnection){return this.api.post<DevelopmentConnection>(`/api/integrations/development/${encodeURIComponent(value.id)}/health`,{expectedVersion:value.version},{ifMatch:value.version});}disconnect(value:DevelopmentConnection){return this.api.post<DevelopmentConnection>(`/api/integrations/development/${encodeURIComponent(value.id)}/disconnect`,{expectedVersion:value.version},{ifMatch:value.version});}rotateCredential(value:DevelopmentConnection,accessToken:string){return this.api.post<DevelopmentConnection>(`/api/integrations/development/${encodeURIComponent(value.id)}/rotate-credential`,{accessToken,expectedVersion:value.version},{ifMatch:value.version});}rotateDevelopmentSecret(value:DevelopmentConnection){return this.api.post<DevelopmentReceipt>(`/api/integrations/development/${encodeURIComponent(value.id)}/rotate-webhook-secret`,{expectedVersion:value.version},{ifMatch:value.version});}}
