export interface JobRole { readonly name: string; readonly permissions: readonly string[]; readonly isActive: boolean; }
export interface BulkJobPage { readonly items: readonly BulkJob[]; readonly page: number; readonly pageSize: number; readonly totalCount: number; }
export interface BulkJob { readonly id: string; readonly projectId: string; readonly type: string; readonly operation?: string | null; readonly dryRun: boolean; readonly state: string; readonly totalItems: number; readonly processedItems: number; readonly succeededItems: number; readonly failedItems: number; readonly cancelRequested: boolean; readonly hasResult: boolean; readonly hasErrorFile: boolean; readonly lastErrorCode?: string | null; readonly lastErrorMessage?: string | null; readonly createdAt: string; readonly startedAt?: string | null; readonly completedAt?: string | null; readonly artifactsExpireAt?: string | null; readonly version: number; }
export interface ImportRow { readonly sourceKey: string; readonly boardId: string; readonly title: string; readonly type: string; readonly priority: string; readonly assigneeUserId: string | null; readonly dueDate: string | null; readonly parentId: string | null; readonly teamId: string | null; readonly customFields: readonly unknown[]; }
export interface ParsedImport { readonly valid: boolean; readonly rows: readonly ImportRow[]; readonly errors: readonly string[]; readonly totalErrors: number; }
export interface JobState { readonly label: string; readonly tone: string; }
